<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
          <item name="colorPrimary">@color/colorbox</item>
          <item name="colorPrimaryDark">@color/colorbox</item>
          <item name="colorAccent">@color/colorbox</item>
    </style>
    <style name="AppThe" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorbox</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorbox</item>
    </style>
</resources>
